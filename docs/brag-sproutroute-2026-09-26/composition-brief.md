# Hyperframes composition brief

Create the 60-second 1920×1080 SproutRoute product film described in `brag-plan.md`. Use the actual React screen captures in `composition/assets`, preserving their content. The user's 60-second request overrides skill defaults and their overnight completion request authorizes rendering after local review.

Source: `src/frontend/src/screens/InputScreen.jsx`, `ResultsScreen.jsx`, mosaic tiles, `PackingChecklist.jsx`, and the saved model-evaluation artifacts. Palette: cream #F5F7EF, ink #15372B, meadow #15803D, sun #FFCA28. Fonts: local Space Grotesk and Inter. Copy must include SproutRoute, “Big trips. Little people.”, and sproutroute.app. Tone: premium, snappy, spunky, with a clear product story. Avoid decorative filler and invented adoption or reliability claims.

Use one deterministic seekable timeline, eight modular story beats plus outro, crisp slide/scale entrances, readable holds, and a consistent camera vocabulary. Reserve the evaluation comparison for the last third; disclose synthetic sample size. The final score is original, at 112 BPM, with soft accents at major scene boundaries. Music and tap effects come from the reproducible local synthesis script, credited in `ASSETS.md`; no third-party music is redistributed. No narration.

Validate with Hyperframes check, inspect the key frames, preview playback, render at 30 fps, and verify 60 seconds with ffprobe. Choose a settled product frame for the poster and bake it into frame zero. Package a local preview and screenshot set suitable for portfolio embedding. Keep all assets credential-free and use fictional trip inputs.
